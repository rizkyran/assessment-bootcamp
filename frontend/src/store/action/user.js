import api from '../../api/api'

export const register = (payload) => {
    return async (dispatch) => {

    }
}

export const register = (payload) => {
    return async (dispatch) => {
        
    }
}

export const register = (payload) => {
    return async (dispatch) => {
        
    }
}
